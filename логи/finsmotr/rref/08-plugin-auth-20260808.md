# Этап 08 — авторизация proxy плагинов

**Дата:** 2026-08-08.  
**Волна:** B. **Зависит от:** 01 не требуется по результату уточнения.  
**Ветка:** `main`. **Коммит:** `9204a3e`. **CI:** не запускался локально; требуется
workflow `Build and Test Intermasq` после push.

## Что сделано
- Установлено, что маршрут `/plugins/<id>/*` монтируется внутри `plugins.Load(r)` — вариант (a); `main.go` маршруты плагинов не регистрирует.
- Reverse-proxy плагинов обёрнут в `auth.Middleware`: неаутентифицированные запросы отклоняются до обращения к sidecar.
- Выбран обычный authenticated-only доступ; admin-only для proxy не требуется.
- Цикла импорта нет: `internal/auth` не импортирует `internal/plugins`.

## Тесты
- `gofmt -l internal/plugins/plugins.go` — ok (пустой вывод).
- `go vet ./internal/plugins/...` — ok.
- `$env:INTERMASQ_SECRET='ci-test-secret-32-bytes-pad-XXXXXXXX'; go test ./internal/plugins/... -count=1` — ok.
- Новые тесты не добавлялись; существующие тесты не изменялись.

## Сводка изменений
- `internal/plugins/plugins.go`: добавлена `auth.Middleware` в цепочку обработчиков `/plugins/<id>/*`.

## Координация / заметки
- `main.go` не изменялся, сигнатура `plugins.Load` не менялась.
- В рабочем дереве до этапа находились чужие изменения; они не включены в коммит этапа.
