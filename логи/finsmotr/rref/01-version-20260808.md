# Этап 01 — W2: единый источник версии

**Дата:** 2026-08-08.
**Волна:** A. **Зависит от:** —.
**Ветка:** `main`. **Коммит:** не создавался в этой сессии. **CI:** не запускался.

## Что сделано

- Добавлен пакет `internal/version` с единственным источником версии и default `1.0.0-pre`.
- Баннер `main.go` и `statusHandler` в `internal/webapi/handlers.go` используют `version.Version`.
- Рецепты production-сборки в `README.md` и `README.en.md` переведены на `-X intermask/internal/version.Version=...`.
- В `README.en.md` исправлено ложное описание дефолтного `INTERMASQ_SECRET`: секрет обязателен, процесс завершается без него.
- Фронтенд не изменялся: версия в UI не показывается, хардкода версии приложения нет.

## Тесты

- `gofmt -l internal/version/version.go main.go internal/webapi/handlers.go` — ok.
- `go vet ./internal/version/... ./internal/webapi/...` — ok.
- `$env:INTERMASQ_SECRET="ci-test-secret-32-bytes-pad-XXXXXXXX"; go test ./internal/version/... ./internal/webapi/... -count=1` — ok (`internal/version`: no test files; `internal/webapi`: PASS).
- Новые тесты не добавлялись; существующие тесты не изменялись.
- Красные тесты для этапа 13: отсутствуют.

## Сводка изменений

- `internal/version/version.go`: новый пакет версии, готовый к переопределению через `-ldflags`.
- `main.go`: баннер читает версию из пакета.
- `internal/webapi/handlers.go`: API status возвращает версию из пакета.
- `README.md`, `README.en.md`: исправлены рецепты `-ldflags` и описание секрета.

## Координация / заметки

- Бинарник и широкие проверки не запускались согласно правилам этапа.
- Commit, push и CI требуют отдельного запуска вне этой сессии.
- Уже существующие изменения `.gitignore` и `coverfunc.txt` не затрагивались.
