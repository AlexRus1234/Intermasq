# Этап 13 — тесты

**Дата:** 2026-08-08.
**Волна:** D'. **Зависит от:** 01–12.
**Ветка:** `main`. **Коммит:** не создан. **CI:** не запускался.

## Статус

**Закрыт по тестовой части.** Прод-код не изменялся.

## Сценарии, отложенные из-за запрета на тесты

| Сценарий (этап) | Ожидаемый тест | Файл |
|---|---|---|
| Версия через `-ldflags` (01) | `TestVersion_Default` | `internal/version/version_test.go` |
| JWT method-check (03) | `TestAuthMiddlewareRejectsNonHMAC` | `internal/auth/auth_test.go` |
| Отзыв токена после изменения пользователя (03) | `TestAuthMiddlewareRevokesTokenAfterUserUpdate` | `internal/auth/auth_test.go` |
| `sudo -n` (04) | `TestSystemdSystemCaller_SudoUsesNonInteractiveFlag` | `internal/initd/system_callers_test.go` |
| Rollback с проверкой dnsmasq (05) | `TestRollbackHandler_Success` | `internal/webapi/linux_test.go` |
| TOCTOU и canonical MAC (06) | существующие host/alias handler tests | `internal/webapi/handlers_test.go` и `dnsmasq_test.go` |
| RBAC (07) | `TestAdminMiddlewareRejectsRegularUser`, `TestAdminMiddlewareAllowsAdmin` | `internal/webapi/rbac_test.go` |
| Plugin auth (08) | `TestPluginRouteRequiresAuthentication` | `internal/plugins/plugins_test.go` |
| Token only in headers (09) | `TestCheckMetricsAuth_TokenQuerySecret`, `TestCheckMetricsAuth_TokenQueryJWT`, `TestMetricsHandler_TokenQuery_200` | `internal/metrics/metrics_test.go` |
| Template mutex (10) | `TestTemplateConcurrentReadWrite` | `internal/templates/templates_test.go` |
| Canonical `NormalizeMAC` (11) | `TestNormalizeMAC` | `internal/validate/validate_test.go` |
| `RWMutex` scan paths (12) | `TestReadScansAreSafeForConcurrentCallers` | `internal/dnsmasq/rwmutex_test.go` |

## Обновлённые red-тесты

- `TestCheckMetricsAuth_TokenQuerySecret` @ `internal/metrics/metrics_test.go`: старое ожидание успеха query-secret заменено на отказ.
- `TestCheckMetricsAuth_TokenQueryJWT` @ `internal/metrics/metrics_test.go`: старое ожидание успеха query-JWT заменено на отказ.
- `TestMetricsHandler_TokenQuery_200` @ `internal/metrics/metrics_test.go`: старое ожидание HTTP 200 заменено на HTTP 401.
- `TestNormalizeMAC` @ `internal/validate/validate_test.go`: uppercase-результат заменён на canonical lowercase.
- `TestRollbackHandler_Success` перенесён из portable `handlers_test.go` в `linux_test.go`; fake dnsmasq обеспечивает успешный `--test`, Windows получает корректный skip.

## Проверки

- Прод-файлы `*.go` не изменялись; изменены только `_test.go`.
- `gofmt -l .` — ok, пустой вывод.
- `go vet ./...` — ok.
- `$env:INTERMASQ_SECRET="ci-test-secret-32-bytes-pad-XXXXXXXX"; go test ./... -count=1` — PASS.
- `-race` не запускался локально согласно общим правилам.
- `-race` оставлен для CI согласно правилам этапа; локальный полный прогон без
  race зелёный.

## Сводка изменений

- `логи/finsmotr/rref/13-tests-20260808.md`: зафиксирован частичный статус,
  список сценариев и red-тестов.

## Координация / заметки

- Post-freeze hotfix прод-кода не потребовался.
