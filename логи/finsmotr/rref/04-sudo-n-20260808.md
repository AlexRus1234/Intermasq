# Этап 04 — sudo без интерактивного запроса

**Дата:** 2026-08-08.
**Волна:** A. **Зависит от:** —.
**Ветка:** main. **Коммит:** 6041e6a.
**CI:** workflow dispatch не запускался из этой сессии.

## Что сделано

- Добавлен флаг `-n` во все sudo-вызовы `Restart` и `RestartSelf` в `internal/initd/system.go`.
- Исправлены все 8 call-site'ов: systemd (`54`, `64`), OpenRC (`118`, `128`), runit (`163`, `174`) и SysVinit (`205`, `215`).

## Тесты

- `gofmt -l internal/initd/system.go` — ok.
- `go vet ./internal/initd/...` — ok.
- `go test ./internal/initd/... -count=1` — ok.
- Новые тесты не добавлялись; существующие тесты не изменялись.

## Сводка изменений

- `internal/initd/system.go`: sudo-команды рестарта теперь запускаются с `sudo -n` и не запрашивают пароль интерактивно.

## Координация / заметки

- Изменён только прод-файл этапа.
- Предсуществующие изменения `.gitignore` и `coverfunc.txt` не включены в коммит.
- Полный CI необходимо запустить через Forgejo workflow `Build and Test Intermasq` с параметрами по умолчанию.
