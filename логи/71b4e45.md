# CI-фиксы и деплой

## Коммит `626d389` — CA trust, git config, Go tarball

- Образ `golang:1.25-alpine` → `node:22-bookworm` (act runner требует node)
- Go устанавливается через `go.dev/dl/go1.25.0.linux-amd64.tar.gz`
- Добавлен шаг `Trust Local CA & Configure Git` перед checkout:
  - `curl -k https://172.20.0.1:9000/roots.pem -o /tmp/step-ca.crt`
  - `git config --global http.sslCAInfo /tmp/step-ca.crt`
- `GOPRIVATE` / `GONOSUMDB` для внутренних модулей
- Upload в release.yml: `curl --cacert` + `--user` вместо `Authorization: token`

## Коммит `3ecf5ba` — gofmt + LF line endings

- `gofmt -w` на всех `.go` файлах (CRLF→LF)
- Создан `.gitattributes`: `*.go text eol=lf`, `*.yml text eol=lf`, `*.json text eol=lf`
- CI `Format check` больше не падает

## Коммит `4627f8d` — NODE_EXTRA_CA_CERTS + SSL_CERT_FILE

- `upload-artifact` action падал: `unable to get local issuer certificate`
- Добавлено в job-level env обоих воркфлоу:
  - `NODE_EXTRA_CA_CERTS: /tmp/step-ca.crt`
  - `SSL_CERT_FILE: /tmp/step-ca.crt`
- Artifact upload заработал

## Коммит `71b4e45` — Package owner: AlexRus1234

- release.yml: `api/packages/Build/generic/...` → `api/packages/AlexRus1234/generic/...`
- Бинарник теперь лежит под пользователем, не в организации Build

## Результат

- CI пайплайн полностью работает: checkout → frontend build → go build → vet → gofmt → test → smoke → artifact upload
- Release пайплайн: вручную с версией → бинарник + sha256 → Forgejo Packages
- Первый релиз v3.1.0 залит на прод, работает
